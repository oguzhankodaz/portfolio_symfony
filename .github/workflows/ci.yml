name: Symfony CI

on:
  pull_request:
    branches:
      - dev

jobs:
  test_symfony:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Test CI
        run: echo "CI çalışıyor deneme yapılıyor"